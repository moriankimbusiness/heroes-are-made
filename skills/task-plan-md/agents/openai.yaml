interface:
  display_name: "Task Plan MD"
  short_description: "Create docs/tasks plan.md files"
  default_prompt: "Use $task-plan-md to create or update docs/tasks/<task-name>/plan.md with detailed AI-only implementation phases, phase-specific code snippets, and a user-only manual QA/debug checklist (preconditions, step-by-step validation, expected results, failure debugging)."
